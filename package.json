{
  "name": "division2-companion",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "catalog:build": "node scripts/build-catalog.mjs",
    "catalog:validate": "node scripts/validate-catalog.mjs",
    "catalog:check": "npm run catalog:build && npm run catalog:validate",
    "dev": "npm run catalog:check && vite",
    "build": "npm run catalog:check && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.2"
  }
}
